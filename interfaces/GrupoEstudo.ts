export default interface GrupoEstudo {
  id: string;
  tema: string;
  dono: string;
  descricao?: string;
}

export interface newGrupoEstudo {
  temaEstudo: string;
  idMinistrante: string;
}
